[project]
name = "mojo-lime"
version = "0.1.0"
description = "LIME local explanation sampling and surrogate fitting implemented in Mojo"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Lee Penkman", email = "leepenkman@gmail.com" }]
keywords = ["mojo", "lime", "explainable-ai", "machine-learning"]
dependencies = ["numpy", "scipy", "scikit-learn", "scikit-image", "tqdm"]

[project.optional-dependencies]
dev = ["pytest", "lime"]

[project.urls]
Homepage = "https://github.com/lee101/mojo-lime"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "python" }

[tool.setuptools.packages.find]
where = ["python"]
